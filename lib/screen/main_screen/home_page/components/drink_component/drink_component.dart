import 'package:flutter/material.dart';

class DrinkComponent extends StatelessWidget {
  const DrinkComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
